function getIngredients() {
    var selecteditems = [];

    $('#checkboxes input:checked').each(function () {
        //selecteditems.push($(ob).val());
        selecteditems.push("dfsajobgsdjgsbjod");

    })
};